// ===========================================================================
// Hidden
// ===========================================================================

var Hidden = this.Hidden = Data.extend({
	type: "hidden",
	
	_setChrome: function() {
		// hidden fields don't need chrome
	}
});
